CONSTANTS
    SUITS = {"H", "D"}
    RANKS = {2, 3, 11, 14}

SPECIFICATION Spec

\* For pure mathematical modules, we can only check that it parses correctly
\* The properties involve infinite sets and can't be model checked directly